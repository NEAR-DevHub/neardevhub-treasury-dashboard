name: Deploy Treasury Factory contract

on:
  workflow_dispatch: # This allows manual triggering
  push:
    branches:
      - main
    paths:
      - 'treasury-factory/**'
      - 'web4/**'

jobs:
  deploy:
    name: Deploy to production
    runs-on: ubuntu-latest
    environment: "treasury-factory.near"
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
      - name: Modify the rust toolchain version
        run: rustup override set 1.86
      - name: Install cargo-near CLI
        run: curl --proto '=https' --tlsv1.2 -LsSf https://github.com/near/cargo-near/releases/latest/download/cargo-near-installer.sh | sh
      - name: Deploy
        run: |
          if [ -z "${{ secrets.PIKESPEAK_API_KEY }}" ]; then
            echo "Error: PIKESPEAK_API_KEY secret is not set."
            exit 1
          fi
          if [ -z "${{ secrets.POSTHOG_API_KEY }}" ]; then
            echo "Error: POSTHOG_API_KEY secret is not set."
            exit 1
          fi
          export PIKESPEAK_API_KEY="abcdefg"
          export POSTHOG_API_KEY="${{ secrets.POSTHOG_API_KEY }}"

          echo "${{ vars.NEAR_SOCIAL_ACCOUNT_ID }}"


          cargo near deploy build-reproducible-wasm  "${{ vars.NEAR_SOCIAL_ACCOUNT_ID }}" \
            without-init-call \
            network-config mainnet \
            sign-with-plaintext-private-key \
            --signer-public-key "ed25519:2Nc7CSqoJKUJ4vWpnB7g5PhdGw3pYUb4LWxdG91nEcAH" \
            --signer-private-key "ed25519:31EHRkuPSvVEBGtvPYWnMUdBFyfcZP2fPcedsEGA5ntdU3JQpPRTovgk3vcV1gQQSctK25C5g45feXmCyyRokJfA" \
            send
        working-directory: ./treasury-factory
