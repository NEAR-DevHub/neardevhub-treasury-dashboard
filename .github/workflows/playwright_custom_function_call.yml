name: Playwright Tests - Custom Function Call

on:
  workflow_dispatch: {}
  workflow_call:
    inputs:
      node-version:
        required: false
        type: string
        default: "20"

concurrency:
  group: custom-function-call-${{ github.ref }}
  cancel-in-progress: true

jobs:
  playwright-tests-custom-function-call:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        shard: [1/1]
        project:
          - treasury-testing

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20
          cache: npm

      - name: Download installed dependencies
        uses: actions/download-artifact@v4
        with:
          name: installed-dependencies
          path: ./

      - name: Extract node_modules
        run: tar -xzf installed-dependencies.tar.gz

      - name: Download Playwright browsers
        uses: actions/download-artifact@v4
        with:
          name: playwright-browsers
          path: ./

      - name: Extract browsers
        run: |
          mkdir -p ~/.cache
          tar -xzf playwright-browsers.tar.gz -C ~/.cache

      - name: Build project
        run: npm run build

      - name: Run tests
        id: custom-function-call-tests
        run: |
          npx playwright test playwright-tests/tests/custom-function-call \
            --project=${{ matrix.project }} \
            --shard=${{ matrix.shard }}

      - name: Sanitize shard name
        run: echo "SHARD_SAFE=$(echo "${{ matrix.shard }}" | sed 's|/|-|')" >> $GITHUB_ENV

      - name: Upload Playwright Test Artifacts (on failure)
        if: failure() && steps.custom-function-call-tests.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: playwright-test-artifacts-custom-function-call-${{ matrix.project }}-shard-${{ env.SHARD_SAFE }}.zip
          path: |
            test-results/
            playwright-report/
          retention-days: 1
